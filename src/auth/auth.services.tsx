/** Handle login
 * Xử lý đăng nhập khi truyền vào email và password
 * @param email
 * @param password
 */
function handleLogin(email: string, password: string) {
  try {
  } catch (error) {
    throw error
  }
}

export { handleLogin }
